// Centralized print configuration for logo sizes
const printConfig = {
  // HTML/CSS sizes (px)
  topLogoPx: 400,
  bottomLogoPx: 120,

  // jsPDF sizes (mm)
  topLogoMm: 80,
  bottomLogoMm: 40,
};

export default printConfig;

